# Database Connection (Prisma)
# For PostgreSQL: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# For local development, we can switch to SQLite if preferred.
DATABASE_URL="postgresql://johndoe:randompassword@localhost:5432/mydb?schema=public"

# AI Provider Configuration
# Options: 'gemini' | 'openai'
AI_PROVIDER="gemini"

# API Keys
GEMINI_API_KEY="your_gemini_api_key_here"
# OPENAI_API_KEY="your_openai_api_key_here" # Only needed if AI_PROVIDER is 'openai'

# Application Settings
LOG_LEVEL="info"
# ENCRYPTION_KEY="your_32_byte_secret_key" # Optional, defaults to dev key if omitted
